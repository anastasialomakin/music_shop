version: '3.8'

services:
  web:
    build: . 
    ports:
      - "8000:5000"
    volumes:
      - .:/app  
    environment:
      - DATABASE_URL=mysql+pymysql://root:secretpassword@db/mydb
      - SECRET_KEY=a_very_secret_key
    depends_on:
      - db

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: secretpassword
      MYSQL_DATABASE: mydb
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: