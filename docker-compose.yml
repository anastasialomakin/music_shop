version: '3.8'

services:
  web:
    build: .

    ports:
      - "5000:5000"
   
    volumes:
      - .:/app
      - db_data:/app/instance
      - ./app/uploads:/app/app/uploads   
    environment:
      - FLASK_APP=main.py
volumes:
  db_data:
  upload_data: